/// System and application lifecycle observation utilities.
///
/// This library provides observers like `MinAppLifecycle` to easily monitor
/// and react to application lifecycle state changes (such as pausing, resuming,
/// or detaching) directly within your state management flow.
library minimals_state_manager.observers;

export 'src/observers/min_app_lifecycle.dart';
